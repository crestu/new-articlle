<h1>Categories</h1>


<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category)%> <%= category.articles.count %></li>
    <%= category.description %> 
  <% end %>
</ul> 

<%= link_to "New Category", new_category_path(@category) %>

